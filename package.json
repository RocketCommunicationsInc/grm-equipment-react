{
  "name": "grm-equipment-react",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@astrouxds/react": "^6.13.0",
    "node-sass": "^7.0.3",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "5.0.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "eject": "react-scripts eject",
    "cy": "cypress open",
    "cy:run": "cypress run",
    "cy:component": "cypress run --component --browser chrome",
    "cy:unit": "cypress run --e2e --browser chrome",
    "test": "npm run cy:unit && npm run cy:component"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest",
      "plugin:cypress/recommended"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cypress": "^10.7.0",
    "eslint-plugin-cypress": "^2.12.1",
    "sass": "^1.54.9"
  }
}
